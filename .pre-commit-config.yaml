repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.11
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(packages|tests)/
      - id: ruff-format
        files: ^(packages|tests)/
